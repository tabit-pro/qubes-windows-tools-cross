From e02446dfa2ea8e6a87a030149fcc049a414252b4 Mon Sep 17 00:00:00 2001
From: Mikhail Lukashov <mlukashov@tabit.pro>
Date: Fri, 22 Oct 2021 12:08:33 +0300
Subject: [PATCH] Fix whitespaces ; Fix typos

---
 src/qrexec-agent/qrexec-agent.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/src/qrexec-agent/qrexec-agent.c b/src/qrexec-agent/qrexec-agent.c
index bb2b946..d9a595a 100644
--- a/src/qrexec-agent/qrexec-agent.c
+++ b/src/qrexec-agent/qrexec-agent.c
@@ -469,7 +469,7 @@ static DWORD InterceptRPCRequest(IN OUT WCHAR *commandLine, OUT WCHAR **serviceC
         {
             // maybe there is an argument appended? look for a file with
             // +argument stripped
-	    WCHAR *newsep = NULL;
+            WCHAR *newsep = NULL;
             newsep = wcschr(serviceName, L'+');
             if (newsep)
             {
@@ -480,7 +480,7 @@ static DWORD InterceptRPCRequest(IN OUT WCHAR *commandLine, OUT WCHAR **serviceC
                 if (!newsep)
                 {
                     // should never happen!
-                    LogError("Fail to contstruct a path for service+arg call");
+                    LogError("Fail to construct a path for service+arg call");
                     return ERROR_PATH_NOT_FOUND;
                 }
                 newsep++;
-- 
2.33.1.windows.1

